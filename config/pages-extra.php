<?php
/** Extra blog posts merged after config/pages.php */
return [
         'blog' => [
             'slug' => 'blog',
             'category' => 'Guides',
             'type' => 'page',
             'nav' => 'Blog',
             'title' => 'Shehanly Blog - Privacy-First Tools, Security & Everyday Workflows',
             'h1' => 'The Shehanly blog',
             'description' => 'Practical guides on using free online tools without leaking data, converting documents safely, generating strong passwords and working faster with developer utilities.',
             'keywords' => 'shehanly blog, private online tools, free developer tools guides, password security, pdf converter safety, jwt decoder, disposable email, base64 encode decode',
             'intro' => 'Short, practical articles from the builder of Shehanly. No fluff. Each post solves one problem and points you at the free tool that does the work.',
             'body' => [
                 ['h2' => 'Latest posts'],
                 'New writing ships here first. Every article is written to rank, to teach, and to send you back into a tool you can actually use.',
                 ['link' => ['href' => '/ip-dns-lookup-guide', 'text' => 'How to look up DNS and IP data without leaking the domain you are investigating (7 Sep 2026)']],
                 ['link' => ['href' => '/url-encoder-guide', 'text' => 'How to URL encode and decode without leaking the query string (6 Sep 2026)']],
                 ['link' => ['href' => '/base64-guide', 'text' => 'How to encode and decode Base64 without uploading the payload (5 Sep 2026)']],
                 ['link' => ['href' => '/word-to-pdf-guide', 'text' => 'How to convert Word to PDF online without leaking the document (4 Sep 2026)']],
                 ['link' => ['href' => '/json-formatter-guide', 'text' => 'How to format and validate JSON without sending the payload (3 Sep 2026)']],
                 ['link' => ['href' => '/hash-generator-guide', 'text' => 'How to generate MD5 and SHA-256 hashes without uploading the file (2 Sep 2026)']],
                 ['link' => ['href' => '/qr-code-guide', 'text' => 'How to generate a QR code without uploading your data (1 Sep 2026)']],
                 ['link' => ['href' => '/temp-mail-guide', 'text' => 'How to use a disposable email without leaking your real inbox (31 Aug 2026)']],
                 ['link' => ['href' => '/jwt-decoder-guide', 'text' => 'How to decode a JWT in your browser without leaking the token (30 Aug 2026)']],
                 ['link' => ['href' => '/pdf-to-word-guide', 'text' => 'How to convert a PDF to Word online without leaking the file (29 Aug 2026)']],
                 ['link' => ['href' => '/strong-password-guide', 'text' => 'How to generate a strong password in your browser (27 Aug 2026)']],
                 ['link' => ['href' => '/private-online-tools-guide', 'text' => 'How to use free online tools without giving away your data (26 Aug 2026)']],
                 ['link' => ['href' => '/education-guides', 'text' => 'Education guides: Excel, Word, PowerPoint and defensive security literacy']],
                 'Want a topic covered next? Use the Contact page and say exactly what you were trying to do when a random tool site made you hesitate.'
             ],
             'faq' => [
                 ['How often is the blog updated?', 'New posts go up when there is a real problem worth solving, not on a fake daily calendar.'],
                 ['Are these posts sales pages?', 'No. The tools stay free. The writing exists to teach and to earn search traffic the honest way.'],
             ],
         ],
    'ip-dns-lookup-guide' => [
        'slug' => 'ip-dns-lookup-guide',
        'category' => 'Guides',
        'type' => 'page',
        'nav' => 'DNS Lookup Guide',
        'published' => '2026-09-07',
        'title' => 'How to Look Up DNS and IP Data Without Leaking the Domain You Are Investigating (2026)',
        'h1' => 'How to look up DNS and IP data without leaking the domain you are investigating',
        'description' => 'A 2026 guide to A, AAAA, MX, TXT, NS and CAA lookups plus IP geolocation: why random whois sites log the domain you type, how a server-side resolver should work, and a 45-second workflow for authorized recon only.',
        'keywords' => 'dns lookup online, mx record checker, ip geolocation, nslookup online, txt record lookup, caa record, whois alternative, shehanly',
        'intro' => 'A domain lookup looks public until you remember the context. A pre-launch hostname, a client staging box, a brand you have not announced, an IP that belongs to an incident you are handling. The moment a random whois site logs that query next to your IP, the investigation is no longer only yours. This guide shows how to read public DNS and IP data through a proxy that rejects private ranges, what the records actually mean, and why this work stays legal only when the target is public or you have written authorization.',
        'body' => [
            'Last updated: 7 September 2026. Written for developers, sysadmins, support engineers and anyone doing authorized, defensive recon. Public DNS is public. Guessing passwords, bypassing auth, or scanning a network you do not own is a different job and it is not this page.',
            ['h2' => 'What a DNS lookup actually returns'],
            'DNS is the phone book of the internet. You ask a resolver for a name. It answers with records. The common ones you will actually use:',
            ['ul' => [
                'A: IPv4 address of a host.',
                'AAAA: IPv6 address of the same host.',
                'CNAME: an alias. Follow it until you hit an A or AAAA.',
                'MX: mail servers and their priority. Lower number is tried first.',
                'TXT: free text. SPF, DKIM, DMARC, domain verification tokens and the occasional leftover secret people should not have published.',
                'NS: the authoritative name servers for the zone.',
                'SOA: serial, refresh and the mailbox that owns the zone.',
                'CAA: which certificate authorities are allowed to issue for the name.',
            ]],
            'None of that is a vulnerability by itself. It is the public contract the domain owner published. Your job is to read it without adding a third party to the conversation.',
            ['h2' => 'Why most online lookup sites are a leak'],
            'Search dns lookup and the first results look like a terminal in a browser. Type a hostname, get records. What they hide is the log. The domain you typed, your IP, the time, sometimes a shareable /lookup/example.com page that stays in their sitemap.',
            'That is fine when the name is google.com. It is not fine when the name is a client staging host, a not-yet-announced product subdomain, or an IP from an incident ticket. Some sites wrap the result in ads and a Recent lookups list. A unique hostname is identifying even after you close the tab.',
            'WHOIS is worse. Registrars already publish a lot. Random whois front-ends add another copy of the query and often scrape contact fields you did not need.',
            ['h2' => 'The safe pattern: resolve on a server you control, never follow private ranges'],
            'A browser cannot run a real recursive lookup the way a public resolver can. ISP cache, split horizon, and stale TTL will lie to you. Shehanly therefore resolves on the server with dns_get_record and a DNS-over-HTTPS fallback. You see what a public resolver sees, not what your hotel Wi-Fi cached.',
            'The proxy rejects RFC1918, loopback and link-local addresses. That is not a feature for attackers. It is SSRF hygiene. A lookup tool that will fetch 127.0.0.1 or 169.254.169.254 is a hole, not a utility.',
            ['link' => ['href' => '/ip-dns-lookup', 'text' => 'Open the IP and DNS lookup']],
            ['h2' => 'A 45-second workflow when a hostname looks wrong'],
            ['ul' => [
                'Copy the hostname only. Strip https://, paths, query strings and a trailing slash.',
                'Open the lookup on shehanly.com. Confirm the URL.',
                'Paste the name. Read A and AAAA first. Then MX if mail is the question. Then TXT if deliverability or a verification token is the question.',
                'If you have an IP instead of a name, look up geolocation and ASN. Treat city-level geo as a hint, not a courtroom fact.',
                'Compare NS and SOA if you suspect a migration or a hijack of nameservers.',
                'Close the tab. You now have the public records. You do not have a second company log of the query sitting next to your home IP.',
            ]],
            ['link' => ['href' => '/ip-dns-lookup', 'text' => 'Look up the records now']],
            ['h2' => 'How to read the records without inventing a story'],
            ['ul' => [
                'Two A records is usually load balancing or a dual-homed host, not a compromise.',
                'A CNAME to a CDN is normal. Follow it. The edge IP will change. That is the product.',
                'MX pointing at Google or Microsoft is expected for those tenants. MX pointing at a random VPS is a finding if the business thinks mail is hosted elsewhere.',
                'A TXT record that starts v=spf1 is mail policy. A missing or p=none DMARC record is weak policy, not proof of a breach.',
                'CAA that lists only one CA means a different CA should refuse to issue. Empty CAA means any CA can issue.',
                'SOA serial that never moves can mean a forgotten zone. It can also mean a hidden primary. Do not guess. Ask the owner.',
            ]],
            'GeoIP is a database guess. A cloud anycast address will show a city that is not where the workload lives. ASN tells you the network operator. That is usually more honest than the city field.',
            ['h2' => 'The errors that waste an afternoon'],
            ['ul' => [
                'Looking up the URL instead of the host. dns_get_record does not want https://app.example.com/login.',
                'Trusting your laptop resolver. Corporate DNS and split horizon will show internal answers that the public internet never sees.',
                'Treating a 24-hour TTL as instant. Old records linger until the TTL dies. Wait, or query an authoritative NS directly.',
                'Confusing WHOIS contacts with DNS. The registrar record and the zone file are different databases.',
                'Calling this a port scan. A DNS lookup is not nmap. Do not slide from public records into unsolicited probing.',
            ]],
            'If the tool returns nothing, the name may not exist, the resolver timed out, or you pasted a private IP the proxy correctly refused. Check the input before you blame the site.',
            ['h2' => 'Legal line. Read it twice.'],
            ['ul' => [
                'Public DNS and public IP metadata are public. Reading them is normal operations and normal due diligence.',
                'You need written authorization before you scan ports, brute credentials, or test a system that is not yours.',
                'Do not use lookup results to harass, dox, or impersonate anyone.',
                'Do not feed the tool RFC1918 addresses hoping it will reach an internal host. It will refuse. That is the design.',
            ]],
            'Shehanly is a utility, not a red-team platform. The education guides cover defensive OSINT literacy. This post covers the lookup itself.',
            ['h2' => 'What a lookup tool must never do'],
            ['ul' => [
                'It must not log the queried name next to your IP in a public recent list.',
                'It must not follow redirects into 127.0.0.1, 10.0.0.0/8, 192.168.0.0/16 or cloud metadata IPs.',
                'It must not claim WHOIS privacy stripping is a hack.',
                'It must not ask you to create an account to see MX records.',
                'It must not wrap the result in a share link that indexes the hostname.',
            ]],
            'A fast test: look up example.com, open DevTools, watch Network. You should see a request to this domain only. If the browser posts the hostname to a third-party whois API with a key in JavaScript, close the tab.',
            ['h2' => 'Pair it with the rest of the stack'],
            ['link' => ['href' => '/url-encoder-guide', 'text' => 'Percent-decode a hostname that arrived inside a query string']],
            ['link' => ['href' => '/hash-generator-guide', 'text' => 'Hash a zone file or export without uploading it']],
            ['link' => ['href' => '/json-formatter-guide', 'text' => 'Pretty-print a DNS-over-HTTPS JSON response']],
            ['link' => ['href' => '/education-guides', 'text' => 'Defensive OSINT literacy: what is public and what is not']],
            ['h2' => 'Why this post sits next to the tool'],
            'People search dns lookup because a hostname failed, mail bounced, or a certificate question showed up in a ticket. The honest page tells them the query itself can be sensitive, which records matter, where the legal line is, and then hands them the tool. That is the Shehanly pattern: rank for the job, teach the risk, finish the job.',
            ['link' => ['href' => '/ip-dns-lookup', 'text' => 'Look up DNS records and IP data']],
            ['link' => ['href' => '/private-online-tools-guide', 'text' => 'How to use free online tools without giving away your data']],
            ['link' => ['href' => '/blog', 'text' => 'Back to the blog index']],
        ],
        'faq' => [
            ['Is a DNS lookup hacking?', 'No. Reading public DNS is normal. Scanning, exploiting or accessing systems without authorization is a different act and it is illegal in most places.'],
            ['Why do results differ from my terminal?', 'Propagation and cache. Your ISP resolver, a public resolver and the authoritative NS can disagree until the old TTL expires.'],
            ['Can I look up private IPs?', 'No. RFC1918, loopback and link-local addresses are rejected so the proxy cannot be used for SSRF.'],
            ['Does Shehanly keep a history of names I searched?', 'There is no account and no public recent-lookups list. The resolver runs on the server because a browser cannot be a public recursive resolver. The name is not turned into a share page.'],
        ],
    ],
    'url-encoder-guide' => [
        'slug' => 'url-encoder-guide',
        'category' => 'Guides',
        'type' => 'page',
        'nav' => 'URL Encode Guide',
        'published' => '2026-09-06',
        'title' => 'How to URL Encode and Decode Without Leaking the Query String (2026)',
        'h1' => 'How to URL encode and decode without leaking the query string',
        'description' => 'A 2026 guide to percent-encoding in the browser: encodeURI vs encodeURIComponent vs form-urlencoded, why pasting a live URL into a random encoder is a leak, and a 30-second workflow that never leaves the tab.',
        'keywords' => 'url encode online, url decode no upload, percent encoding, encodeURIComponent vs encodeURI, form urlencoded plus, decode query string locally, shehanly',
        'intro' => 'A URL looks public until you remember what people hide in it: search terms, reset tokens, invite codes, session leftovers, a customer id sitting next to a signature. The moment an encoder posts that string to print %20, the query is no longer only yours. This guide shows how to percent-encode and decode in the tab, which mode to pick, and why plus and %20 are not the same job.',
        'body' => [
            'Last updated: 6 September 2026. Written for developers, support engineers, QA and anyone who just copied a broken link out of a ticket and needs the original characters without handing the whole address to a stranger.',
            ['h2' => 'What percent-encoding actually does'],
            'A URL can only carry a safe subset of ASCII. Space, emoji, Sinhala, a slash that is data and not a path, a question mark that belongs inside a value: all of those must become a percent sign and two hex digits. Space becomes %20. A raw & inside a parameter value becomes %26 so it does not split the query.',
            'That is encoding, not encryption. Anyone can reverse it. Treat a percent-encoded query as sensitive as the plaintext it hides.',
            ['h2' => 'Why most online URL encoders are a leak'],
            'Search url encode and the first results look harmless. Paste, click Encode, copy. What they hide is the POST. A password-reset link, an OAuth redirect with a code, a signed download URL with a customer id is now a copy on a box you do not run.',
            'Some tools keep Recent URLs. Some wrap the result in a share link. A unique token in a query string is identifying even after you close the tab. Support teams paste production callbacks into converters every day. That is how secrets leave the company without a ticket.',
            ['h2' => 'The safe pattern: encode in the tab'],
            'Shehanly encodes and decodes with encodeURI, encodeURIComponent and a form-urlencoded mode that turns spaces into plus. A query inspector splits the decoded address into keys and values so you can read the parameters without a second tool. Nothing is posted. Watch Network while you paste a 4 KB callback. You should see static assets only.',
            ['link' => ['href' => '/url-encoder', 'text' => 'Open the browser URL encoder']],
            ['h2' => 'Pick the mode before you hit Encode'],
            ['ul' => [
                'encodeURIComponent: one parameter value. This is the default. It encodes &, =, ?, # and slash so the value cannot break the URL around it.',
                'encodeURI: a whole address. It leaves : / ? # and the structural characters alone so https:// stays a URL.',
                'application/x-www-form-urlencoded: HTML form bodies and some older query styles. Space becomes + instead of %20. Decode must turn + back into space first.',
            ]],
            'Wrong mode is the number one afternoon-killer. encodeURI on a single value leaves the ampersand raw and the query splits. encodeURIComponent on a full URL turns https:// into noise. Form mode on a path that never used plus invents spaces that were never there.',
            ['h2' => 'A 30-second workflow when a link looks broken'],
            ['ul' => [
                'Copy the raw string only. Do not copy surrounding quotes or a trailing period from the email.',
                'Open the encoder on shehanly.com. Confirm the URL.',
                'Paste. If you already see %2F or %20, hit Decode. If the string has spaces or non-ASCII, hit Encode.',
                'Choose component when you are encoding one value. Choose uri when the whole address must stay clickable. Choose form when the source was a POST body or an old + spaced query.',
                'Read the query table if the output is a full URL. Confirm the token, the id, the redirect.',
                'Copy the result and close the tab. The original paste never left the machine.',
            ]],
            ['link' => ['href' => '/url-encoder', 'text' => 'Encode or decode locally now']],
            ['h2' => 'The errors that waste an afternoon'],
            ['ul' => [
                'Double encoding. %20 encoded again becomes %2520. Decode once. If you still see percent digits, decode again on purpose, not by habit.',
                'Plus versus %20. In a path, + is a plus. In form-urlencoded, + is a space. Pick the mode that matches the source.',
                'Smart quotes and a trailing slash copied from Slack. They look invisible and break decodeURIComponent.',
                'Encoding a whole callback with component mode. The slashes die and the browser can no longer parse the address.',
                'Leaving a raw space in a query. Some servers accept it. Email clients and chat apps will split the link at the space.',
            ]],
            'If decode throws URI malformed, a percent sign is missing its two hex digits. You copied a truncated log line. Restore the missing bytes or cut the string at the last complete % pair.',
            ['h2' => 'What a URL tool must never do'],
            ['ul' => [
                'It must not POST the textarea to print percent digits.',
                'It must not create a public /p/abc123 link of your last paste.',
                'It must not call the output encrypted.',
                'It must not phone home with a sample of query keys for product analytics.',
                'It must not ask you to log in to copy the result.',
            ]],
            'A fast test: paste https://example.com/?q=hello world, open DevTools, watch Network. If a request carries the address, close the tab.',
            ['h2' => 'Pair it with the rest of the stack'],
            ['link' => ['href' => '/base64-guide', 'text' => 'Base64-decode a wrapped value before you percent-decode it']],
            ['link' => ['href' => '/json-formatter-guide', 'text' => 'Pretty-print JSON you pulled out of a query parameter']],
            ['link' => ['href' => '/jwt-decoder-guide', 'text' => 'Decode a JWT that arrived as a URL fragment']],
            ['link' => ['href' => '/hash-generator-guide', 'text' => 'Hash the canonical query string without uploading it']],
            ['h2' => 'Why this post sits next to the tool'],
            'People search url encode because a link broke in transit or a parameter needs to travel. The honest page tells them the paste is often a secret, which JavaScript function matches the job, why plus is not always a space, and then hands them the tool. That is the Shehanly pattern: rank for the job, teach the risk, finish the job.',
            ['link' => ['href' => '/url-encoder', 'text' => 'URL encode and decode locally']],
            ['link' => ['href' => '/private-online-tools-guide', 'text' => 'How to use free online tools without giving away your data']],
            ['link' => ['href' => '/blog', 'text' => 'Back to the blog index']],
        ],
        'faq' => [
            ['What is the difference between encodeURI and encodeURIComponent?', 'encodeURI is for a whole address and leaves structural characters alone. encodeURIComponent is for one value and encodes &, =, ?, # and slash so the value cannot break the URL.'],
            ['Does Shehanly upload my URL?', 'No. Encode and decode run in the browser. Watch the Network tab if you want proof.'],
            ['Why does decode fail with URI malformed?', 'A lone % without two hex digits, a truncated copy-paste, or a string that was already cut by a chat client. Fix the percent pair and try again.'],
            ['When should I use the + space mode?', 'When the source is application/x-www-form-urlencoded: HTML forms and some older query strings. Paths and modern query values usually want %20.'],
        ],
    ],
    'base64-guide' => [
        'slug' => 'base64-guide',
        'category' => 'Guides',
        'type' => 'page',
        'nav' => 'Base64 Guide',
        'published' => '2026-09-05',
        'title' => 'How to Encode and Decode Base64 Without Uploading the Payload (2026)',
        'h1' => 'How to encode and decode Base64 without uploading the payload',
        'description' => 'A 2026 guide to Base64 encode and decode in the browser: why pasting a token into a random converter is a leak, how UTF-8 and URL-safe alphabets actually work, and a 30-second workflow that never leaves the tab.',
        'keywords' => 'base64 encode online, base64 decode no upload, url safe base64, utf-8 base64 converter, decode base64 locally, shehanly',
        'intro' => 'Base64 looks like noise until you remember what people paste into it: basic-auth headers, JWT segments, data URIs, API tokens wrapped for a config file. The moment a converter posts that string to print readable text, the secret is no longer only yours. This guide shows how to encode and decode in the tab, when to flip the URL-safe alphabet, and why Base64 is never encryption.',
        'body' => [
            'Last updated: 5 September 2026. Written for developers, pentesters, support engineers and anyone who just copied a long string out of a log and needs the original bytes without handing them to a stranger.',
            ['h2' => 'What Base64 actually does'],
            'Base64 turns raw bytes into 64 printable ASCII characters so the payload can sit in email, JSON, HTML or a query string without breaking the container. Every 3 bytes become 4 characters. Padding with = fills the last block. That is the whole trick.',
            'It is an encoding. Anyone can reverse it. There is no key. If the input was a password, the output is the same password wearing a costume. Treat a Base64 blob as sensitive as the bytes it hides.',
            ['h2' => 'Why most online Base64 sites are a leak'],
            'Search base64 decode and the first results look harmless. Paste, click Decode, copy. What they hide is the POST. A bearer token, a .pem wrapped as text, a customer export stuffed into a data URI is now a copy on a box you do not run.',
            'Some tools keep Recent strings. Some wrap the result in a share link. A unique token is identifying even after you close the tab. Support teams paste production headers into converters every day. That is how credentials leave the company without a ticket.',
            ['h2' => 'The safe pattern: encode in the tab'],
            'Shehanly encodes and decodes with TextEncoder, btoa and atob in the browser. UTF-8 is the default so Sinhala, Arabic and emoji survive the round trip. URL-safe mode swaps + and / for - and _ and drops padding so the value can sit in a query string. Nothing is posted. Watch Network while you paste a 200 KB dump. You should see static assets only.',
            ['link' => ['href' => '/base64', 'text' => 'Open the browser Base64 encoder']],
            ['h2' => 'A 30-second workflow when a log line looks like garbage'],
            ['ul' => [
                'Copy the raw string only. Do not copy surrounding JSON quotes unless they belong to the payload.',
                'Open the encoder on shehanly.com. Confirm the URL.',
                'Paste. If it looks like Base64 already, hit Decode. If it is plain text you need to wrap, hit Encode.',
                'Tick URL safe when the string must live in a query parameter or a filename.',
                'Copy the result and close the tab. The original paste never left the machine.',
            ]],
            ['link' => ['href' => '/base64', 'text' => 'Encode or decode locally now']],
            ['h2' => 'Standard versus URL-safe versus UTF-8'],
            ['ul' => [
                'Standard Base64: email attachments, HTTP basic auth, classic data URIs. Alphabet is A-Z a-z 0-9 + / with = padding.',
                'URL-safe Base64: JWT payloads, signed cookies, query values. Alphabet swaps + / for - _ and often drops padding.',
                'UTF-8: the character set of the original text. A Latin-1 decoder will mangle every non-English string and then blame Base64.',
            ]],
            'JWTs are three Base64URL segments joined by dots. Decode the middle segment in this tool if you only need the claims. Use the JWT decoder when you also want exp rendered as a local date.',
            ['h2' => 'The errors that waste an afternoon'],
            ['ul' => [
                'Missing padding. Some libraries strip =. Add them back until the length is a multiple of 4, or use a decoder that does it for you.',
                'Newlines from email. MIME Base64 wraps at 76 characters. Strip whitespace before you decode.',
                'Mixing alphabets. A URL-safe string fed to a strict + / decoder dies on the first hyphen.',
                'Calling it encryption. A junior ticket that says we Base64 the API key is a finding, not a control.',
                'Smart quotes and trailing spaces copied from Slack. They look invisible and break atob.',
            ]],
            'If decode prints garbage with black-diamond characters, you decoded the right Base64 into the wrong character set, or the source was binary and you asked for text. Download the bytes instead of forcing a string.',
            ['h2' => 'What a Base64 tool must never do'],
            ['ul' => [
                'It must not POST the textarea to print ASCII.',
                'It must not create a public /p/abc123 link of your last paste.',
                'It must not call the output encrypted.',
                'It must not phone home with a sample of strings for product analytics.',
                'It must not ask you to log in to copy the result.',
            ]],
            'A fast test: paste SGVsbG8=, open DevTools, watch Network. If a request carries the string, close the tab.',
            ['h2' => 'Pair it with the rest of the stack'],
            ['link' => ['href' => '/jwt-decoder-guide', 'text' => 'Decode a JWT in the browser without leaking the token']],
            ['link' => ['href' => '/json-formatter-guide', 'text' => 'Pretty-print the JSON you just unwrapped']],
            ['link' => ['href' => '/hash-generator-guide', 'text' => 'Hash the original bytes without uploading them']],
            ['link' => ['href' => '/url-encoder', 'text' => 'Percent-encode the result if it still has to travel in a URL']],
            ['h2' => 'Why this post sits next to the tool'],
            'People search base64 decode because a string landed and they cannot read it. The honest page tells them the paste is often a secret, why the converter must stay in the tab, which alphabet to pick, and then hands them the tool. That is the Shehanly pattern: rank for the job, teach the risk, finish the job.',
            ['link' => ['href' => '/base64', 'text' => 'Encode and decode Base64 locally']],
            ['link' => ['href' => '/private-online-tools-guide', 'text' => 'How to use free online tools without giving away your data']],
            ['link' => ['href' => '/blog', 'text' => 'Back to the blog index']],
        ],
        'faq' => [
            ['Is Base64 encryption?', 'No. It is a reversible encoding. Anyone with the string can recover the original bytes. Use a real cipher if the payload must stay secret.'],
            ['Does Shehanly upload my text?', 'No. Encode and decode run in the browser with TextEncoder and atob. Watch the Network tab if you want proof.'],
            ['Why does decode fail?', 'Wrong alphabet, missing padding, leftover whitespace, or a truncated string. Strip spaces, restore = until the length is a multiple of 4, then try again.'],
            ['When do I use URL-safe Base64?', 'When the value sits in a URL, a filename or a JWT. The standard + and / characters break those containers.'],
        ],
    ],
];
